[
{% for iface in node_item.networking.interfaces %}
{% if iface_item.mac_address is defined %}
{
  "is_connected": True, "mac_address": "{{ iface.mac_address }}", "subnet": {"name": "{{ iface.nutanix_network | default(provider.configuration.prism_network_name) }}"}
}{%- if not loop.last %},{% endif %}
{% endif %}
{% endfor %}
]